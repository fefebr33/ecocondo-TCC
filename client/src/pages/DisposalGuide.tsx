import PageIntro from "@/components/PageIntro";
import { Badge } from "@/components/ui/badge";
import { trpc } from "@/lib/trpc";
import { AlertTriangle, CheckCircle2, Recycle } from "lucide-react";

const colors: Record<string, string> = { reciclavel: "bg-[#e8f4ed] text-[#0f7350]", organico: "bg-[#f2f0df] text-[#77802c]", rejeito: "bg-[#f1f2f1] text-[#526057]", eletronico: "bg-[#e9f0fb] text-[#285fa1]", perigoso: "bg-[#fff0e7] text-[#b45522]" };
export default function DisposalGuide() { const { data: guides, isLoading } = trpc.guides.list.useQuery(); return <div><PageIntro eyebrow="Educação ambiental" title="Guia de descarte" description="Orientações para separar materiais de maneira mais segura e colaborar com a qualidade da coleta seletiva." /><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{isLoading ? <p className="text-sm text-muted-foreground">Carregando guia...</p> : guides?.map((guide) => <article key={guide.id} className="rounded-[24px] border border-[#dce8e0] bg-white p-5 shadow-[0_16px_34px_-28px_rgba(4,66,42,.32)]"><div className="flex items-start justify-between gap-3"><span className={`grid h-11 w-11 place-items-center rounded-2xl ${colors[guide.wasteType] || "bg-[#e8f4ed] text-[#0f7350]"}`}><Recycle className="h-5 w-5" /></span><Badge className="border-0 bg-[#f4f7f5] text-[10px] font-bold tracking-[.08em] text-muted-foreground hover:bg-[#f4f7f5]">{guide.wasteType.toUpperCase()}</Badge></div><h2 className="mt-5 text-lg font-bold tracking-[-.035em]">{guide.title}</h2><p className="mt-3 text-sm leading-6 text-muted-foreground">{guide.instructions}</p><div className="mt-5 space-y-3"><div className="rounded-xl bg-[#f1f8f3] p-3"><p className="flex items-center gap-1.5 text-xs font-bold text-[#0b6647]"><CheckCircle2 className="h-3.5 w-3.5" />Pode descartar</p><p className="mt-1 text-xs leading-5 text-[#426558]">{guide.acceptedItems}</p></div><div className="rounded-xl bg-[#fff5ed] p-3"><p className="flex items-center gap-1.5 text-xs font-bold text-[#a95427]"><AlertTriangle className="h-3.5 w-3.5" />Evite misturar</p><p className="mt-1 text-xs leading-5 text-[#80614f]">{guide.rejectedItems}</p></div></div></article>)}</section></div>; }
