import PageIntro from "@/components/PageIntro";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { Gift, Medal, Plus, Sparkles } from "lucide-react";
import { FormEvent, useState } from "react";
import { toast } from "sonner";

const rewardForm = { title: "", description: "", pointsCost: "", stock: "" };

export default function Engagement() {
  const utils = trpc.useUtils();
  const profile = trpc.profile.me.useQuery();
  const { data: ranking } = trpc.engagement.ranking.useQuery();
  const { data: rewards } = trpc.engagement.rewards.useQuery();
  const createReward = trpc.engagement.createReward.useMutation({ onSuccess: () => { utils.engagement.rewards.invalidate(); toast.success("Recompensa adicionada ao catálogo."); setForm(rewardForm); setIsFormOpen(false); }, onError: (issue) => toast.error(issue.message) });
  const redeem = trpc.engagement.redeem.useMutation({ onSuccess: () => { utils.engagement.ranking.invalidate(); utils.engagement.rewards.invalidate(); utils.profile.me.invalidate(); toast.success("Resgate solicitado com sucesso."); }, onError: (issue) => toast.error(issue.message) });
  const [isFormOpen, setIsFormOpen] = useState(false);
  const [form, setForm] = useState(rewardForm);
  const isAdmin = profile.data?.role === "administrador";
  const isResident = profile.data?.role === "morador";
  function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); createReward.mutate({ title: form.title, description: form.description, pointsCost: Number(form.pointsCost), stock: form.stock ? Number(form.stock) : null }); }
  return <div><PageIntro eyebrow="Participação" title="Engajamento" description="Valorize a participação de moradores com pontos, ranking e recompensas alinhadas à rotina do condomínio." action={isAdmin ? <Button onClick={() => setIsFormOpen((open) => !open)} className="h-10 rounded-xl bg-[#0f7350] font-semibold text-white hover:bg-[#0a6243]"><Plus className="mr-2 h-4 w-4" />Nova recompensa</Button> : undefined} /><div className="grid gap-5 xl:grid-cols-[.9fr_1.1fr]"><section className="rounded-[24px] border border-[#dce8e0] bg-white p-5 shadow-[0_16px_34px_-28px_rgba(4,66,42,.35)] sm:p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#fff3df] text-[#af7726]"><Medal className="h-5 w-5" /></span><div><p className="font-semibold">Ranking do condomínio</p><p className="text-sm text-muted-foreground">Pontuação acumulada por coletas concluídas.</p></div></div>{ranking?.length ? <ol className="mt-5 divide-y divide-[#edf2ef]">{ranking.slice(0, 8).map((resident) => <li key={resident.id} className="flex items-center justify-between py-3"><span className="flex items-center gap-3"><b className={`grid h-8 w-8 place-items-center rounded-xl text-xs ${resident.position <= 3 ? "bg-[#fff3df] text-[#9c691e]" : "bg-[#f0f5f2] text-muted-foreground"}`}>{resident.position}</b><span><span className="block text-sm font-semibold">{resident.name}</span><span className="text-xs text-muted-foreground">Bloco {resident.block} · {resident.apartment}</span></span></span><span className="text-sm font-bold text-[#0f7350]">{resident.points} pts</span></li>)}</ol> : <p className="mt-6 text-sm leading-6 text-muted-foreground">O ranking será formado quando as primeiras coletas recicláveis forem concluídas.</p>}</section><section className="rounded-[24px] border border-[#dce8e0] bg-white p-5 shadow-[0_16px_34px_-28px_rgba(4,66,42,.35)] sm:p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#e8f4ed] text-[#0f7350]"><Gift className="h-5 w-5" /></span><div><p className="font-semibold">Catálogo de recompensas</p><p className="text-sm text-muted-foreground">Resgates disponíveis para moradores ativos.</p></div></div>{isFormOpen && <form onSubmit={submit} className="mt-5 grid gap-3 rounded-2xl border border-[#cfe1d7] bg-[#f7fbf8] p-4 sm:grid-cols-2"><label className="grid gap-1.5 text-xs font-semibold sm:col-span-2">Título<Input required value={form.title} onChange={(event) => setForm({ ...form, title: event.target.value })} className="h-10 rounded-xl bg-white" /></label><label className="grid gap-1.5 text-xs font-semibold sm:col-span-2">Descrição<Input required value={form.description} onChange={(event) => setForm({ ...form, description: event.target.value })} className="h-10 rounded-xl bg-white" /></label><label className="grid gap-1.5 text-xs font-semibold">Pontos necessários<Input required type="number" min="1" value={form.pointsCost} onChange={(event) => setForm({ ...form, pointsCost: event.target.value })} className="h-10 rounded-xl bg-white" /></label><label className="grid gap-1.5 text-xs font-semibold">Estoque (opcional)<Input type="number" min="0" value={form.stock} onChange={(event) => setForm({ ...form, stock: event.target.value })} className="h-10 rounded-xl bg-white" /></label><div className="flex gap-2 sm:col-span-2"><Button disabled={createReward.isPending} className="h-10 rounded-xl bg-[#0f7350] text-white hover:bg-[#0a6243]">Salvar recompensa</Button><Button type="button" variant="ghost" onClick={() => setIsFormOpen(false)} className="h-10 rounded-xl">Cancelar</Button></div></form>}<div className="mt-5 grid gap-3 sm:grid-cols-2">{rewards?.length ? rewards.map((reward) => <article key={reward.id} className="rounded-2xl border border-[#e0ebe4] bg-[#fbfdfc] p-4"><div className="flex items-start justify-between gap-3"><span className="grid h-9 w-9 place-items-center rounded-xl bg-[#e8f4ed] text-[#0f7350]"><Sparkles className="h-4 w-4" /></span><Badge className="border-0 bg-[#edf7f1] text-[#0a7048] hover:bg-[#edf7f1]">{reward.pointsCost} pts</Badge></div><h2 className="mt-4 text-sm font-bold">{reward.title}</h2><p className="mt-1 text-xs leading-5 text-muted-foreground">{reward.description}</p><div className="mt-4 flex items-center justify-between"><span className="text-[11px] text-muted-foreground">{reward.stock === null ? "Estoque aberto" : `${reward.stock} disponível(is)`}</span>{isResident ? <Button size="sm" disabled={redeem.isPending || (reward.stock !== null && reward.stock < 1)} onClick={() => redeem.mutate({ rewardId: reward.id })} className="h-8 rounded-lg bg-[#0f7350] text-xs text-white hover:bg-[#0a6243]">Resgatar</Button> : null}</div></article>) : <p className="py-8 text-sm text-muted-foreground sm:col-span-2">O administrador pode montar o primeiro catálogo de recompensas nesta área.</p>}</div></section></div></div>;
}
