import PageIntro from "@/components/PageIntro";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { trpc } from "@/lib/trpc";
import { ShieldCheck, UsersRound } from "lucide-react";
import { FormEvent, useEffect, useState } from "react";
import { toast } from "sonner";

export default function Settings() { const utils = trpc.useUtils(); const condominium = trpc.condominium.current.useQuery(); const members = trpc.profile.members.useQuery(); const updateCondominium = trpc.condominium.update.useMutation({ onSuccess: () => { utils.condominium.current.invalidate(); utils.profile.me.invalidate(); toast.success("Dados do condomínio atualizados."); }, onError: (issue) => toast.error(issue.message) }); const setRole = trpc.profile.setRole.useMutation({ onSuccess: () => { utils.profile.members.invalidate(); toast.success("Perfil atualizado."); }, onError: (issue) => toast.error(issue.message) }); const [form, setForm] = useState({ name: "", address: "", city: "", state: "", blockCount: 1 }); useEffect(() => { if (condominium.data) setForm({ name: condominium.data.name, address: condominium.data.address || "", city: condominium.data.city || "", state: condominium.data.state || "", blockCount: condominium.data.blockCount }); }, [condominium.data]); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); updateCondominium.mutate({ ...form, address: form.address || null, city: form.city || null, state: form.state ? form.state.toUpperCase() : null }); } return <div><PageIntro eyebrow="Administração" title="Configurações" description="Ajuste os dados gerais do condomínio e acompanhe os perfis de acesso existentes na plataforma." /><div className="grid gap-5 xl:grid-cols-[.8fr_1.2fr]"><section className="rounded-[24px] border border-[#dce8e0] bg-white p-5 shadow-[0_16px_34px_-28px_rgba(4,66,42,.35)] sm:p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#e8f4ed] text-[#0f7350]"><ShieldCheck className="h-5 w-5" /></span><div><p className="font-semibold">Dados do condomínio</p><p className="text-sm text-muted-foreground">Informações visíveis nos relatórios.</p></div></div><form onSubmit={submit} className="mt-5 grid gap-3"><label className="grid gap-1.5 text-xs font-semibold">Nome<Input required value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} className="h-10 rounded-xl bg-[#fbfdfc]" /></label><label className="grid gap-1.5 text-xs font-semibold">Endereço<Input value={form.address} onChange={(event) => setForm({ ...form, address: event.target.value })} className="h-10 rounded-xl bg-[#fbfdfc]" /></label><div className="grid grid-cols-[1fr_70px] gap-3"><label className="grid gap-1.5 text-xs font-semibold">Cidade<Input value={form.city} onChange={(event) => setForm({ ...form, city: event.target.value })} className="h-10 rounded-xl bg-[#fbfdfc]" /></label><label className="grid gap-1.5 text-xs font-semibold">UF<Input maxLength={2} value={form.state} onChange={(event) => setForm({ ...form, state: event.target.value })} className="h-10 rounded-xl bg-[#fbfdfc]" /></label></div><label className="grid gap-1.5 text-xs font-semibold">Quantidade de blocos<Input required type="number" min="1" max="99" value={form.blockCount} onChange={(event) => setForm({ ...form, blockCount: Number(event.target.value) })} className="h-10 rounded-xl bg-[#fbfdfc]" /></label><Button disabled={updateCondominium.isPending} className="mt-2 h-10 rounded-xl bg-[#0f7350] text-white hover:bg-[#0a6243]">Salvar configurações</Button></form></section><section className="rounded-[24px] border border-[#dce8e0] bg-white p-5 shadow-[0_16px_34px_-28px_rgba(4,66,42,.35)] sm:p-6"><div className="flex items-center gap-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#e8f4ed] text-[#0f7350]"><UsersRound className="h-5 w-5" /></span><div><p className="font-semibold">Perfis de acesso</p><p className="text-sm text-muted-foreground">Defina a responsabilidade de cada pessoa autenticada no EcoCondo.</p></div></div>{members.data?.length ? <div className="mt-5 divide-y divide-[#edf2ef]">{members.data.map(({ profile, user, resident }) => <article key={profile.id} className="flex flex-col gap-3 py-4 sm:flex-row sm:items-center sm:justify-between"><div><p className="text-sm font-semibold">{resident?.name || user?.name || "Usuário"}</p><p className="mt-0.5 text-xs text-muted-foreground">{user?.email || "Sem e-mail disponível"}</p></div><div className="flex items-center gap-2"><Badge className="border-0 bg-[#edf7f1] text-[#0a7048] hover:bg-[#edf7f1]">{profile.role}</Badge><select value={profile.role} onChange={(event) => setRole.mutate({ userId: profile.userId, role: event.target.value as "administrador" | "coletor" | "morador" })} disabled={setRole.isPending} className="h-9 rounded-lg border border-[#dce8e0] bg-white px-2 text-xs font-semibold outline-none focus:ring-2 focus:ring-[#0f7350]/30"><option value="administrador">Administrador</option><option value="coletor">Coletor</option><option value="morador">Morador</option></select></div></article>)}</div> : <p className="mt-6 text-sm text-muted-foreground">Os perfis aparecerão quando os usuários realizarem o primeiro acesso.</p>}</section></div></div>; }
